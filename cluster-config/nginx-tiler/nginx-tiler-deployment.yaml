apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose --file docker-compose.yml convert
    kompose.version: 1.17.0 (a74acad)
  creationTimestamp: null
  labels:
    io.kompose.service: nginx-tiler
  name: nginx-tiler
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: nginx-tiler
    spec:
      containers:
      - image: raster-foundry-nginx-tiler
        name: nginx-tiler
        ports:
        - containerPort: 443
        resources: {}
        volumeMounts:
        - mountPath: /srv/dist/
          name: nginx-tiler-claim0
        - mountPath: /etc/nginx/nginx.conf
          name: nginx-tiler-claim1
        - mountPath: /etc/nginx/includes/
          name: nginx-tiler-claim2
        - mountPath: /etc/nginx/conf.d/default.conf
          name: nginx-tiler-claim3
      restartPolicy: Always
      volumes:
      - name: nginx-tiler-claim0
        persistentVolumeClaim:
          claimName: nginx-tiler-claim0
      - name: nginx-tiler-claim1
        persistentVolumeClaim:
          claimName: nginx-tiler-claim1
      - name: nginx-tiler-claim2
        persistentVolumeClaim:
          claimName: nginx-tiler-claim2
      - name: nginx-tiler-claim3
        persistentVolumeClaim:
          claimName: nginx-tiler-claim3
status: {}
