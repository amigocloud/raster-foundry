apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose --file docker-compose.yml convert
    kompose.version: 1.17.0 (a74acad)
  creationTimestamp: null
  labels:
    io.kompose.service: nginx-backsplash
  name: nginx-backsplash
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: nginx-backsplash
    spec:
      containers:
      - image: raster-foundry-nginx-backsplash
        name: nginx-backsplash
        ports:
        - containerPort: 443
        resources: {}
        volumeMounts:
        - mountPath: /srv/dist/
          name: nginx-backsplash-claim0
        - mountPath: /etc/nginx/nginx.conf
          name: nginx-backsplash-claim1
        - mountPath: /etc/nginx/includes/
          name: nginx-backsplash-claim2
        - mountPath: /etc/nginx/conf.d/default.conf
          name: nginx-backsplash-claim3
      restartPolicy: Always
      volumes:
      - name: nginx-backsplash-claim0
        persistentVolumeClaim:
          claimName: nginx-backsplash-claim0
      - name: nginx-backsplash-claim1
        persistentVolumeClaim:
          claimName: nginx-backsplash-claim1
      - name: nginx-backsplash-claim2
        persistentVolumeClaim:
          claimName: nginx-backsplash-claim2
      - name: nginx-backsplash-claim3
        persistentVolumeClaim:
          claimName: nginx-backsplash-claim3
status: {}
